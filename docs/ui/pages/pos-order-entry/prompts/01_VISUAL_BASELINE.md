# Codex Prompt - Phase 1: POS Order Entry Visual Baseline

Begin only after Phase 0, scope, and visual direction are approved and the pack
is `implementation-ready`.

Renew the real `/pos` implementation in place. Preserve the live local-user
loader, employee eligibility/attribution, Zod and local-pos contract validation,
site-agent/db-pos ownership, draft creation, three order types, optional note,
redirect to item entry, shared shell/status polling, and existing tests. Do not
use fixtures.

Apply approved references only to hierarchy, density, spacing, proportions,
responsive composition, and tone. Reuse `@yuta/ui`, semantic tokens, Lucide,
IBM Plex Sans, and the approved service-time POS shell. Do not add navigation,
fields, authentication, permissions, APIs, schema, offline writes, cloud data,
or printer behavior.

Report exact files, checks, browser evidence, deviations, and risks. Stop before
Phase 2.
