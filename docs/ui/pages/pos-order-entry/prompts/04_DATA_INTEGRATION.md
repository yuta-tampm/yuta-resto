# Codex Prompt - Phase 4: POS Order Entry Data Audit

Map the approved UI again to current local users, create-order input/response,
Server Action, site-agent service, and db-pos order/user schema. An existing
visual renewal normally needs no data rewrite.

Preserve `apps/yuta-pos -> apps/site-agent -> packages/db-pos`, local-only data,
active non-kitchen attribution, UUIDv7/draft creation, and redirect. Stop for
approval before any field, enum, permission, authentication flow, API, contract,
idempotency rule, schema/migration, cloud relation, runtime, or device change.

Run only affected existing checks. Stop before Phase 5.
