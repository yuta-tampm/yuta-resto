# Codex Prompt - Phase 0: Repository Analysis Gate

Do not change runtime code. Re-run the inventory in `README.md`, verify the
local-session and sign-out boundary, confirm the exact real route inventory,
capture the authenticated current screen, resolve every shared-context layer
and exact shell mode, update `DESIGN_HANDOFF.md`, and stop. Do not generate a
mockup or start Phase 1 without explicit approval.
