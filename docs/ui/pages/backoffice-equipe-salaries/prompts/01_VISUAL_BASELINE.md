# Codex Prompt — Phase 1: Typed-Fixture UI Discovery Prototype

Start only after Phase 0 scope and visual direction are approved and the
package is `implementation-ready`.

Build only the approved responsive prototype in the current Backoffice shell,
using typed fictional fixtures and a demonstration-data notice. Create no real
contracts, repositories, actions, permissions, entitlements, schema,
migrations, providers, or persistence.

Reuse `@yuta/ui`, tokens, Geist Sans, Lucide icons, and
`REUSE_APPROVED_SHARED_SHELL`. Cover only approved local interactions and
truthful states. Omit/disable deferred OCR, remuneration, payroll, Formalités,
contract generation, apprenticeship, register, and PDF behavior.

Documents Wave A is the only exception: add the reviewed sixth dossier tab with
typed fictional examples, a persistent demonstration notice, and disabled add,
view, and replace controls. Create no document IDs, URLs, storage keys,
contracts, actions, permissions, providers, schema, migration, API, or
persistence.
Verify 1440/1024/768/390, accessibility, dialogs, and overflow; report and stop.
