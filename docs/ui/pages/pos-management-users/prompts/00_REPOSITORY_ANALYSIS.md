# Codex Prompt - Phase 0: Repository Analysis Gate

Do not change runtime code. Re-run the repository inventory in `README.md`,
verify the local-session/site-agent/db-pos chain, capture the authenticated real
screen, resolve every shared-context layer and exact shell mode, update the
design handoff, and stop. Fixture replacement is forbidden. Do not generate a
mockup or start Phase 1 without explicit approval.
