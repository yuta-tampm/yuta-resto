# Codex Prompt — Phase 0: Repository Analysis Gate

Do not change runtime code. Read the repository and this page package, verify
the complete Implementation Inventory, resolve all four shared UI context
layers, select exactly one shell mode, capture the authenticated existing page,
and prepare the design prompt/context bundle. Preserve local session,
site-agent/db-pos ownership, contracts, actions, transactions, active-rule
locks, combo/payment invariants, and real tests. Fixture replacement is
forbidden. Stop after Phase 0 and request approval.
