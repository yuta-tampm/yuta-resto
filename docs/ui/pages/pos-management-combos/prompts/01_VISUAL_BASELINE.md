# Codex Prompt — Phase 1: Visual Baseline

Begin only after explicit Phase 0 and visual-reference approval. Refactor the
existing `/management/combos` route in place. Reuse the approved shared
`ManagementHeader`, apply only the approved hierarchy/density/responsive
direction, and preserve the real loader, local session, Server Actions,
contracts, validation, mutations, error states, and tests. Do not use fixtures
or change database/API/auth/device boundaries. Stop and report before Phase 2.
