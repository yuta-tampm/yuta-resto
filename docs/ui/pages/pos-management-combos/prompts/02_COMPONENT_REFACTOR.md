# Codex Prompt — Phase 2: Component Responsibilities

Begin only after explicit Phase 2 approval. Extract route-local components by
stable UI responsibility where Phase 1 proves reuse or complexity. Keep combo
business rules in contracts/core/site-agent, keep the route server-first, and
avoid speculative shared components or a new framework. Preserve behavior and
run focused regression checks. Stop before Phase 3.
