# Codex Prompt — Phase 4: POS Kitchen Data Integration

Do not begin without separate approval for a data/service change. The normal UI
renewal should keep the existing local-pos contracts and site-agent ownership.

If the newest-100/N+1 limitation is selected for work, first approve a
server-owned Kitchen read contract that applies service day, station/status,
counts, ordering, and pagination before returning bounded data. If command
hardening is selected, separately specify concurrency, order locks,
transactions, replay/idempotency, audit/attribution, and error contracts.

Never add cloud tenancy/sync, browser database access, fixtures, device paths,
printer routing/settings, or presentation-owned calculations. Run affected
contracts, site-agent, db-pos, POS, architecture, offline, and integration
checks. Stop before Phase 5.
