# Codex Prompt — Phase 0: Repository Analysis Gate

Do not change application/runtime code, schema, migrations, contracts, data, or
print jobs. Re-read the repository authorities and this package, then verify
route absence, Management shell/session behavior, local singleton patterns,
site-agent/db-pos ownership, and the complete customer-receipt snapshot,
renderer, preview, retry, and reprint paths.

Return the 23-section Implementation Inventory required by
`docs/ui/templates/page/prompts/00_REPOSITORY_ANALYSIS.md`. Explicitly classify
this as `PAGE`, `NEW_PAGE`, integrated proposal, and
`NEW_CAPABILITY_DISCOVERY`; use `NOT_APPLICABLE` for the missing target baseline
and inspect only safe containing-shell evidence. Reconcile drift with this pack.

List every undecided validation, permission, audit, concurrency, empty/rename,
payload-version, and downstream-consumer decision. Complete the shared-context
matrix and self-contained design prompt, then stop. Do not generate a design or
advance Phase 1.
