# Phase 02 — Optional Typed-Fixture Structure

Run only with separate Phase 2 approval. Phase 00 must pass first.

If product requests an in-browser prototype, create the smallest development-
only route-local composition using `ManagementHeader`, existing `@yuta/ui`
primitives, and explicitly fictional typed values. Keep the hub card
unavailable. Clearly label the screen as a prototype, block it outside
development, and add focused presentation tests.

Do not call site-agent, use real IDs/data, add contracts/APIs/schema/migrations,
change auth/session/shell, or present fixtures as production blueprint. Stop
after reporting files/checks and fixture-removal obligations.
