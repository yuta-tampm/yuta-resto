Change: platform-admin-formalites-template-authority-foundation
Stage: Post-apply Knowledge Formatting Remediation
Classification: FORMAT_ONLY
Review status: APPROVED
Created: 2026-09-06T23:19:29.4524147+02:00
Approval source: explicit current-user instruction
Approval recorded by: Codex workflow
Approved: 2026-09-06T23:24:55.7569499+02:00
Approved formatter-diff SHA-256: 8eedc61c60c3ce03f0443486460795a526b5f3cf4d1ccfc802a2e9d56832a8c2
Apply status: COMPLETED
Completed: 2026-09-06T23:25:35.2588797+02:00
Workflow status: DONE

# Formatting Remediation Review

## Authorization boundary

The previously approved semantic Knowledge diff remains applied and was not
rolled back. This packet proposes only the Prettier output for the exact five
canonical targets authorized by Control Tower. No formatter output has been
applied to those targets.

Target set: `5` exact files.

## Exact target set and hashes

| Exact target                                   | Pre-remediation SHA-256                                            | Expected post-remediation SHA-256                                  |
| ---------------------------------------------- | ------------------------------------------------------------------ | ------------------------------------------------------------------ |
| `docs/features/identity-access/README.md`      | `1d36f7a43a574f1992983022a1f4ed5bd2609d12f5652ea4ac2cc6f33cfcb6f0` | `6df71086f496151b4fa4f8e286a6e3db83233db140a9d154ff4c85adaa653e36` |
| `docs/architecture/IDENTITY_AND_MEMBERSHIP.md` | `dc56d157de118a368991a570e3f29c15d96d00051795b909a89bf4b3365d0401` | `515e3ab673914db49e01aeda1bda7c05f7053628435107566df8fdeb19ca1578` |
| `docs/architecture/OVERVIEW.md`                | `b41460c72391c4febbc1f41b9f8826d033b387dfcd8fce0a024710bf083b2f7e` | `ec83802f61facff522007346ca0ec2111698dea6ac81211cf19289bd944824c5` |
| `docs/CURRENT_STATE.md`                        | `0357250fff345d2079a93995affd85cebdfb3afafe5c22ce14207086b9095389` | `25929ae727be862b7af4370aec1d881a135b609d6529a1deabaf98f3f23874f4` |
| `docs/MODULE_REGISTRY.md`                      | `6c2e8a9d294b1e726ae05cc82cc09df9a50ca7af275a19c40a5b95295d6acb3e` | `dd18644278f44bdce8ce900ca0b6bb183551ea5f016c53d5c241f47dd7628942` |

Any target-set or pre-remediation hash drift invalidates this packet and must
stop formatter application.

## Exact proposed formatter diff

The complete proposed formatter-only byte patch is:

`docs/reviews/platform-admin-formalites-template-authority-foundation/05-formatting-remediation.diff`

SHA-256:
`8eedc61c60c3ce03f0443486460795a526b5f3cf4d1ccfc802a2e9d56832a8c2`.

The patch includes Prettier's CRLF-to-LF normalization for all five targets and
table alignment changes where applicable. Because the repository has
`core.autocrlf=true`, byte-exact applicability was checked with:

```powershell
git -c core.autocrlf=false apply --check --whitespace=nowarn docs/reviews/platform-admin-formalites-template-authority-foundation/05-formatting-remediation.diff
```

Result: `PASS`.

A separate temporary patch application reproduced all five expected
post-remediation hashes exactly.

## Semantic equivalence

Semantic diff: `NONE`.

The proposed output was generated by running repository Prettier only over the
five target copies. Comparison after normalizing whitespace and Markdown table
separator widths is exact for every target. No wording, identifier, link,
lifecycle value, readiness value, permission, grant, ownership, or product
meaning changes.

The exact normative operation identifiers remain present once each, and the
two rejected identifiers remain absent:

- `formalites.template.read`
- `formalites.template.draft.manage`
- `formalites.template.review.submit`
- `formalites.template.publish`
- `formalites.template.retire`

The normative main spec remains unchanged at SHA-256
`3b53193f63cc3536d00506826d5281aea7607a01604112051edebef5bb810db2`.

## Safe proposed-output validation

The formatter output was overlaid onto a short-path detached temporary
worktree. The already-synced main spec was copied into that simulation solely
so documentation-link validation saw the same repository state. Neither the
canonical targets nor the canonical main spec was changed by the simulation.

| Check                                            | Exact result                  |
| ------------------------------------------------ | ----------------------------- |
| Prettier check on all five proposed-output files | `PASS`                        |
| `pnpm docs:check` in safe simulation             | `PASS` — 36 current documents |
| `pnpm architecture:check` in safe simulation     | `PASS`                        |
| Formatter patch target set                       | `PASS` — exactly five files   |
| Expected post-hash reproduction                  | `PASS` — 5/5 exact            |

An initial clean-HEAD simulation reported only broken links because that
temporary worktree did not yet contain the untracked, already-synced main spec.
After mirroring that unchanged main spec, the faithful simulation passed. This
was a simulation setup issue, not a formatter or canonical-repository failure.

## Safeguards

This proposal does not:

- modify any canonical file now;
- roll back or alter the approved semantic Knowledge wording;
- touch a target outside the exact five-file set;
- change the normative main spec;
- promote Product Decision, lifecycle, readiness, environment, or production
  state;
- change roles, grants, tenant authorization, ownership, persistence, legal
  governance, generation, signature, or Documents handoff;
- create or enable `apps/platform-admin`;
- perform deployment or another production action.

## Final apply and validation

Control Tower approved this packet and exact formatter-diff hash. The patch was
applied with `core.autocrlf=false` so the reviewed LF-normalized bytes were
reproduced exactly.

Final results:

- changed canonical target set: PASS — exactly the five approved files;
- exact formatter diff equivalence: PASS — reverse check succeeds;
- expected post-remediation hashes: PASS — 5/5 exact;
- scoped Prettier check: PASS — 5/5 files;
- `pnpm docs:check`: PASS — 36 current documents;
- `pnpm architecture:check`: PASS;
- normative main-spec hash unchanged: PASS;
- exact five normative operation identifiers preserved: PASS;
- rejected operation identifiers absent: PASS;
- semantic wording, lifecycle, readiness, permissions, ownership, runtime, and
  production state unchanged: PASS.

The Git line-ending warning that LF would be converted if Git later rewrites
these files is informational; current bytes match all five approved expected
hashes and pass Prettier.

Formatting remediation: `COMPLETED`.

Ready to apply formatting remediation: `APPLIED_AND_VALIDATED`.
