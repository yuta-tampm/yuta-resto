# Kế hoạch triển khai F07 — Reconstructable Personnel value history

`UI_AFFECTING: YES`

`BROWSER_QA_REQUIRED: YES`

Tài liệu này đồng thời là Tasks và Implementation Plan của change. Việc tạo tài liệu này không cho phép Apply, không thay đổi trạng thái lifecycle và không cho phép production rollout. Trước Apply phải xác nhận lại các đường dẫn dự kiến bên dưới không có thay đổi ngoài phạm vi so với baseline đã ghi nhận.

## 1. Foundation / Data

### TECHNICAL IMPLEMENTATION CONTRACT

- **Scope:** thêm contract versioned cho sáu semantic group; thêm aggregate history, group children và cutover completion marker do Personnel sở hữu; tạo migration additive và đường cutover local có thể chạy lại an toàn.
- **Exact repository areas allowed to change:** `packages/contracts/src/personnel/index.ts`, `packages/contracts/test/personnel.test.ts`, `packages/db-cloud/src/schema/personnel.ts`, `packages/db-cloud/src/schema/index.ts` chỉ khi export hiện hữu cần cập nhật, `packages/db-cloud/src/personnel-history-cutover.ts` nếu cần module riêng, `packages/db-cloud/src/index.ts` chỉ cho export server-side cần thiết, migration mới kế tiếp trong `packages/db-cloud/drizzle/`, metadata Drizzle do lệnh generate tạo, và focused tests trong `packages/db-cloud/test/`. Không sửa migration đã tồn tại.
- **Canonical owner and boundaries:** `@yuta/contracts` sở hữu transport-safe Zod contracts; `@yuta/db-cloud` sở hữu cloud schema, migration, repository và cutover persistence; dữ liệu tiếp tục thuộc Personnel, scoped bởi organization + establishment + employee. Authorities: `AGENTS.md`, `packages/contracts/AGENTS.md`, `packages/db-cloud/AGENTS.md`, `docs/AUTHORITY_MODEL.md`, `docs/features/personnel/README.md`, approved `spec.md`, approved `design.md` và Sensitive Design Gate.
- **Invariants:** payload có version; semantic group chỉ thuộc allowlist Identity, Role, Contract terms, Work time, Entry, Departure; strict validation ở cả write/read; unknown version fail closed; raw JSON không trở thành public/UI contract; event/group là immutable business evidence; baseline đúng một lần cho mỗi dossier tồn tại tại cutover và chỉ là current-at-cutover; dossier tạo sau cutover không có baseline giả; migration additive, không rewrite legacy audit; retention eligibility được suy ra từ authoritative departure, không có keep-forever flag.
- **Required tests/evidence:** contract tests cho mọi version/group và rejection của extra field/unknown version; schema constraint review; generated SQL review; guarded disposable-db integration evidence cho scope/uniqueness/foreign keys, immutable write surface, retention eligibility và toàn bộ cutover matrix; hashes/counts của local synthetic cutover evidence.
- **Stop conditions:** dừng và quay lại Design/Product nếu cần owner mới, shared retention contract, new legal-hold authority, new permission, cross-runtime storage, partial cutover commit, sửa migration đã deploy hoặc payload ngoài allowlist. Dừng Apply nếu intended paths có thay đổi không thể tách khỏi work của user.
- **Explicit out of scope:** production migration/cutover, cleanup executor/cron/job, production backup/PITR approval, quyết định deletion so với irreversible anonymization, legal-hold source mới, reconstruction trước cutover, Documents/Register/Formalités/Planning/Pointage/AI/OCR/authentication/consultation storage.

- [x] 1.1 Mở rộng Personnel contracts bằng discriminated, versioned strict schemas cho sáu group snapshots, per-group classification/metadata input và safe history DTO; verify mọi group, explicit `null`, extra-field rejection, unknown-version rejection và absence của raw persistence JSON bằng `pnpm --filter @yuta/contracts test` và `pnpm --filter @yuta/contracts typecheck`.
- [x] 1.2 Thêm additive Personnel history event/group/cutover-marker schema với full composite scope, event-kind checks, mutation/baseline metadata checks, unique event-group, unique scoped operation và partial unique exactly-one baseline; verify schema bằng focused unit/schema assertions và `pnpm --filter @yuta/db-cloud typecheck`.
- [x] 1.3 Chạy `pnpm db:cloud:generate` để tạo migration mới kế tiếp, review exact generated SQL và metadata để chứng minh chỉ thêm F07 tables/constraints/indexes, không sửa migration cũ hoặc table của module khác; lưu reviewed migration diff làm evidence.
- [x] 1.4 Tạo Personnel-owned cutover runner dùng exclusive scoped transaction lock, fixed `cutoverAt`, all-or-nothing baseline insert/count verification và completion marker; feature-aware writes dùng matching shared scoped lock và fail closed khi marker version còn thiếu; verify bằng guarded disposable-db integration tests cho initial cutover, exactly-one baseline, retry before commit, retry after committed/lost response, concurrent employee creation, concurrent employee update, concurrent departure, transaction failure rollback và missing completion marker fail closed.
- [x] 1.5 Thêm tested retention-eligibility data behavior dựa trên authoritative establishment departure: attached và chưa đủ 5 năm vẫn retained, đủ 5 năm trở thành eligible trừ approved override supplied by an existing future privacy boundary; verify không có cleanup mutation, cron/job, invented legal-hold source hoặc keep-forever field trong schema/diff.

## 2. Service / Domain

### TECHNICAL IMPLEMENTATION CONTRACT

- **Scope:** mở rộng F03 update và departure repository paths để derive changed groups, validate metadata theo từng group, ghi authoritative values/history/compatibility audit/receipt trong một transaction; thêm unified newest-50 history projection và server action mapping an toàn.
- **Exact repository areas allowed to change:** `packages/db-cloud/src/personnel-repository.ts`, module Personnel helper mới trong `packages/db-cloud/src/` chỉ khi extraction làm rõ transaction/cutover/history ownership, `packages/db-cloud/test/personnel-repository.integration.test.ts`, `packages/contracts/src/personnel/index.ts` và `packages/contracts/test/personnel.test.ts` cho contract đã lên kế hoạch, `apps/backoffice/src/app/(authenticated)/equipe/salaries/actions.ts`, cùng focused Backoffice tests trong route hoặc `apps/backoffice/test/`. `apps/backoffice/src/server/auth/permissions.ts` là invariant read-only và không được sửa nếu không quay lại gate.
- **Canonical owner and boundaries:** `packages/db-cloud` sở hữu atomic Personnel mutation/history; Backoffice server action chỉ rederive trusted session context, enforce existing permission và map validated DTO. Authorities: root/package/app `AGENTS.md`, `docs/architecture/DATABASE_BOUNDARIES.md`, `docs/architecture/TENANCY.md`, `docs/architecture/AUTHENTICATION.md`, `docs/AUTHORITY_MODEL.md`, `docs/features/personnel/README.md`, approved Specs/Design.
- **Invariants:** server tự derive exact changed groups từ current authoritative row và proposed values; classification/effective date/reason validate độc lập theo từng group; một group sai từ chối toàn mutation và không tạo partial history; CHANGE của Role/Contract terms/Work time cần ngày quá khứ hoặc hôm nay, không trước authoritative entry và không sau departure; Entry chỉ CORRECTION; không future scheduling; mutation, mọi F07 children, compatibility audit và command receipt commit/rollback cùng nhau; stale revision và existing idempotency semantics giữ nguyên; no-op không tạo history.
- **Required tests/evidence:** focused domain/contract tests cho toàn classification/reason/date matrix, coupled snapshots và multi-group all-or-nothing; guarded repository integration tests cho success, history-write failure rollback, stale revision, idempotent replay/fingerprint conflict, tenant denial, departure record/correct/cancel và audit/F07/receipt consistency.
- **Stop conditions:** dừng nếu transaction hiện hữu không thể bao trọn bốn evidence types, nếu cần browser-supplied authority, permission mới, MANAGER access, cross-module write, new API ownership hoặc thay đổi departure semantics đã duyệt.
- **Explicit out of scope:** scheduled/pending/future activation, payroll/remuneration, Documents/Register/Formalités histories, access/consultation/auth audit, history search/filter/pagination/export, production enablement.

- [x] 2.1 Xây dựng pure Personnel group-diff/snapshot/metadata validation dùng strict contracts: validate từng changed group, reject extra/missing metadata, áp correction-reason matrix và business-date boundaries; verify bằng focused tests gồm atomic Identity CORRECTION + Role CHANGE + Work-time CORRECTION và từng failure branch khiến toàn input bị từ chối.
- [x] 2.2 Tích hợp update F03 vào một transaction gồm scoped current read/revision check, changed-group validation, dossier CAS, một F07 event với mọi group children, compatibility audit và command receipt; verify success/rollback/stale revision/no-op/idempotent replay/request-fingerprint conflict bằng `pnpm --filter @yuta/db-cloud test` cùng guarded `personnel-repository.integration.test.ts` khi disposable cloud database khả dụng.
- [x] 2.3 Tích hợp departure recording/correction/cancellation vào cùng F07 atomic pattern, giữ reason bắt buộc cho correction/cancellation và legacy departure audit; verify previous/new date, `null` cancellation, first recording, stale revision, replay và rollback tests.
- [x] 2.4 Mở rộng unified Personnel history read để parse strict versioned payload, fail closed với unknown/corrupt version, trộn legacy + F07 + cutover baseline, loại compatibility duplicate theo operation, sort ổn định và trả newest 50 + truncation; verify legacy-only, F07-only, mixed, duplicate suppression, 51-event window, neutral system attribution và không lộ raw JSON/scope/operation/revision internals.
- [x] 2.5 Cập nhật Backoffice server actions để tiếp tục require `personnel.employee.manage` cho writes và `personnel.employee.read` cho Historique, rederive trusted tenant context và chỉ trả safe DTO; verify OWNER success cùng MANAGER/STAFF, suspended membership, wrong organization, wrong establishment và foreign employee fail-closed cases bằng focused Backoffice/DB tests.

## 3. UI / Components

### TECHNICAL IMPLEMENTATION CONTRACT

- **Scope:** mở rộng editor F03 để OWNER khai báo semantics theo từng changed group và mở rộng `Historique` hiện hữu để trình bày reconstructable evidence/baseline; không tạo trang hoặc timeline thứ hai.
- **Exact repository areas allowed to change:** `apps/backoffice/src/app/(authenticated)/equipe/salaries/_components/employee-edit-dialog.tsx`, `apps/backoffice/src/app/(authenticated)/equipe/salaries/_components/salaries-page.tsx`, focused route-local component/helper files dưới `_components/` hoặc `_lib/`, `apps/backoffice/src/app/(authenticated)/equipe/salaries/salaries-model.ts` khi cần safe presentation mapping, và focused tests cạnh các file đó hoặc trong `apps/backoffice/test/`. Chỉ cập nhật stable page pack `docs/ui/pages/backoffice-equipe-salaries/`; không sửa `packages/ui` trừ khi một nhu cầu domain-neutral đã được chứng minh và được gate lại.
- **Canonical owner and boundaries:** Backoffice Salaries route sở hữu Personnel presentation; reusable primitives vẫn thuộc `@yuta/ui`. Authorities: `apps/backoffice/AGENTS.md`, `docs/ui/YUTA_FRONTEND_RULES.md`, `docs/ui/BACKOFFICE_FRONTEND_RULES.md`, stable Salaries page pack, `packages/ui/src/index.ts`, approved Specs/Design.
- **Invariants:** French user-facing text; raw JSON/tenant IDs/operation IDs/revision internals không render; current F03 fields, CDD branches, drawer/full-dossier behavior và OWNER-only visibility được giữ; UI metadata theo từng group không tự quyết định changed-field truth; cutover được ghi rõ là snapshot current-at-cutover, không bịa previous/classification/actor; legacy events vẫn đọc được; newest-50/no search/filter/pagination/export giữ nguyên.
- **Required tests/evidence:** component/model tests cho group sections, conditional fields, safe value formatting, explicit null, legacy/F07/baseline timeline, duplicate-free projection, neutral attribution và absence của excluded controls/data; targeted Backoffice typecheck/tests.
- **Stop conditions:** dừng nếu design cần field/enum/domain rule mới, shared UI primitive change, new route, separate history owner, new permission, or display of unvalidated/raw persistence payload.
- **Explicit out of scope:** shell/navigation redesign, Documents/Register/Formalités UI, consultations/access history changes, manager/read-only expansion, export/search/filter/pagination, scheduled-change UI, production labels or legal-compliance claims.

- [x] 3.1 Mở rộng edit dialog theo exact changed semantic groups với lựa chọn CORRECTION/CHANGE và conditional effective-date/reason inputs đúng matrix; verify component/model tests chứng minh Identity dual classification, Entry correction-only, required past-or-today date cho applicable CHANGE, required reasons và không render scheduled/future controls.
- [x] 3.2 Mở rộng `Historique` hiện hữu bằng typed presentation cho event-level grouping và từng semantic group: previous/new values, classification, applicable date/reason, actor, recorded time; verify legacy events vẫn hiển thị, compatibility event không lặp, explicit empty value rõ ràng và raw JSON/internal identifiers không xuất hiện.
- [x] 3.3 Thêm cutover-baseline presentation với wording tiếng Pháp trung lập nói rõ “giá trị hiện tại tại thời điểm kích hoạt”, system attribution và không có invented previous/classification/effective date/reason; verify focused UI tests cho baseline cùng pre-cutover legacy event.
- [x] 3.4 Giữ bounded history surface tối đa 50 event và current truncation message, không thêm search/filter/pagination/export; verify DOM/component assertions và `pnpm --filter @yuta/backoffice test`.

## 4. Interaction / States

### TECHNICAL IMPLEMENTATION CONTRACT

- **Scope:** nối per-group editor metadata vào một atomic submit hiện hữu; xử lý validation, pending, conflict, success, error/retry và post-save history refresh trong drawer lẫn full dossier.
- **Exact repository areas allowed to change:** route-local Salaries client components và helpers tại `apps/backoffice/src/app/(authenticated)/equipe/salaries/_components/` và `_lib/`, `actions.ts` chỉ cho action state/validated boundary đã nêu ở Phase 2, cùng focused tests hiện hữu như `employee-edit-flow.test.ts` và `employee-history-refresh.test.ts` hoặc file test route-local có tên rõ nghĩa.
- **Canonical owner and boundaries:** route-local client state thuộc Backoffice Salaries UI; authoritative validation/mutation vẫn ở server/repository. Authorities: Backoffice/frontend rules, current page pack, approved Specs/Design và Phase 2 contract.
- **Invariants:** một submit cho toàn F03 mutation; browser metadata không tạo authority; server rejection giữ input và gắn lỗi vào group/field phù hợp; invalid group không để nhóm khác được lưu; stale revision giữ recovery hiện hữu; success đóng editor theo quyết định UI hiện hữu, refresh dossier và invalidates/reloads active history; modal/drawer focus, dirty-close protection và keyboard behavior không thoái lui.
- **Required tests/evidence:** interaction tests cho dirty state, conditional reset, multi-group submit, validation preservation, future-date rejection, pending/double-submit, conflict reload, server failure/retry, committed success/history refresh và focus recovery; real-route Browser QA planned for OWNER at 1440, 1024, 768 và 390 px.
- **Stop conditions:** dừng nếu UI cần local optimistic authoritative values, partial group saves, autosave, background activation, or changes to existing modal/drawer global primitives.
- **Explicit out of scope:** separate mutation per group, partial success, autosave, offline queue, scheduled activation/timer/job, consultation paging changes, new visual framework or raw colors.

- [x] 4.1 Nối normalized per-group metadata vào một update action submission và map server errors về group/field, đồng thời giữ user input khi validation thất bại; verify missing/extra metadata, future/effective-bound errors và one-invalid-group/no-partial-save interaction tests.
- [x] 4.2 Bảo vệ pending/double-submit và existing idempotency identity, giữ stale-revision reload/recovery và generic save-error retry; verify không tạo lần lưu/history thứ hai qua focused interaction/action tests.
- [x] 4.3 Sau commit thành công, đóng/reset editor theo flow hiện hữu, refresh authoritative dossier và reload `Historique` nếu đang mở trong cả drawer và full dossier; verify committed values/details xuất hiện ngay, modal không giữ stale data và focus trở lại đúng trigger.
- [x] 4.4 Kiểm tra keyboard/focus/dirty-close behavior và responsive composition của conditional controls trước Browser QA; verify bằng component tests và targeted Backoffice typecheck/test, không thay đổi shared modal/select implementation ngoài scope.

## 5. Integration / Regression

### TECHNICAL IMPLEMENTATION CONTRACT

- **Scope:** chứng minh toàn change khớp Specs/Design, không vượt module/runtime/security boundary; cập nhật current Personnel/page-pack docs theo as-built; thực hiện local technical VERIFY và Browser QA bằng dữ liệu giả/an toàn; chuẩn bị evidence cho Gate 3 nhưng không sync/archive/deploy.
- **Exact repository areas allowed to change:** focused tests đã liệt kê ở các phase; `docs/features/personnel/README.md`; stable `docs/ui/pages/backoffice-equipe-salaries/` files/references; `docs/reviews/personnel-reconstructable-value-history/qa/`; VERIFY/Gate 3 evidence chỉ khi toàn bộ checks và QA đạt theo workflow. Không sửa Product lifecycle registry hoặc production readiness để tự nâng trạng thái.
- **Canonical owner and boundaries:** Personnel remains owner; VERIFY follows `$yuta-run-change`; QA follows `docs/YUTA_QA_PROTOCOL.md`. Authorities additionally include `docs/README.md`, `docs/CURRENT_STATE.md`, `docs/operations/PRODUCTION_READINESS.md`, OpenSpec validation output và current scoped git diff.
- **Invariants:** trusted organization + establishment + employee and OWNER-only remain fail closed; no writes to Documents, Registre, Formalités, Planning, Pointage, AI/OCR, authentication or consultation histories; legacy and F07 history coexist without duplicate; rollback after F07 data means preserve tables/data, block incompatible old writers and roll forward; no destructive automatic rollback; no production enablement/cleanup/backup claim.
- **Required tests/evidence:** Technical Compliance Matrix `Spec requirement -> Design decision -> implementation location -> test/evidence`; phase-contract compliance matrix; strict OpenSpec validation; contract/db/backoffice tests, guarded disposable-db integration suite, typechecks, architecture/docs/format checks and Backoffice build; scoped diff/migration review; Browser QA report + screenshot manifest + SHA-256 screenshots for real local route and required states/viewports.
- **Stop conditions:** do not claim `TECHNICAL IMPLEMENTATION COMPLIANCE: PASS`, `VERIFY: PASS` or `QA: PASS` with missing/failed evidence; stop on environment-blocked required integration/Browser QA, unexplained scoped drift, cross-tenant exposure, missing cutover marker behavior, migration mismatch or any approved-boundary deviation. Do not create ready Gate 3 until all three required PASS values exist.
- **Explicit out of scope:** production deploy/migration/cutover, real employee data QA, production cleanup executor, new legal-hold authority, keep-forever, production backup/PITR approval, normative spec sync/archive, any excluded module or cross-runtime behavior.

- [x] 5.1 Hoàn thiện guarded integration suite trên disposable cloud database cho atomic mutation/history/audit/receipt, multi-group validation, tenant isolation, departure compatibility, newest-50/deduplication và đủ chín cutover cases của Phase 1; verify bằng `pnpm --filter @yuta/db-cloud test` và focused `pnpm --filter @yuta/db-cloud exec vitest run test/personnel-repository.integration.test.ts` khi required guarded database environment đã được cấu hình, ghi chính xác PASS/FAIL/BLOCKED evidence.
- [x] 5.2 Chạy targeted và broader repository checks thực sự tồn tại: `pnpm --filter @yuta/contracts test`, `pnpm --filter @yuta/contracts typecheck`, `pnpm --filter @yuta/db-cloud typecheck`, `pnpm --filter @yuta/backoffice test`, `pnpm --filter @yuta/backoffice typecheck`, `pnpm --filter @yuta/backoffice build`, `pnpm docs:check`, `pnpm architecture:check`, `pnpm -r --if-present typecheck`, `pnpm format:check` và `pnpm exec openspec validate personnel-reconstructable-value-history --strict --json`; record exact exit results without hiding unrelated/pre-existing failures.
- [x] 5.3 Review generated migration and a deterministic change-scoped diff, prove no old migration was edited, no excluded module/runtime/permission was changed, no cleanup job exists, and F07-aware writers fail closed after history activation rather than allowing destructive rollback; retain exact file list and hashes as VERIFY evidence.
- [x] 5.4 Tạo Technical Compliance Matrix ánh xạ từng Spec requirement `->` approved Design decision `->` exact implementation location `->` test/evidence, cùng phase Technical Implementation Contract matrix; chỉ ghi chính xác `TECHNICAL IMPLEMENTATION COMPLIANCE: PASS` và `VERIFY: PASS` khi mọi applicable row đạt.
- [x] 5.5 Đồng bộ current Personnel knowledge và stable Salaries page pack theo as-built behavior mà không nâng Environment/Production Readiness/External Dependency; verify links/content bằng `pnpm docs:check` và page-pack consistency checks đang tồn tại nếu package dùng chúng.
- [x] 5.6 Thực hiện Browser QA trên real local `/equipe/salaries` bằng authenticated OWNER và dữ liệu giả/an toàn tại 1440, 1024, 768 và 390 px: single/multi-group success, per-group validation failure, future-date rejection, conflict/recovery, baseline, mixed legacy/F07 history, newest-50 truncation, loading/error/success, keyboard/focus và no overflow; tạo `QA_REPORT.md`, `screenshot-manifest.md`, SHA-256 screenshots và chỉ ghi `QA: PASS` khi toàn evidence đạt. Authorization/cross-establishment denial tiếp tục được chứng minh ở technical tests, không tạo MANAGER access để QA.
- [x] 5.7 Khi và chỉ khi implementation, Technical Compliance, VERIFY và required QA đều PASS, chuẩn bị Gate 3 packet theo `$yuta-run-change` với `Sync authorization: PENDING`; stop để human review, không sync main specs, archive, deploy hoặc production-enable.
